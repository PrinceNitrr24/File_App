<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File App</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-cyan-300">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            type="text"
            class="block px-5 py-2 bg-black text-white w-full rounded-md outline-none shadow-lg shadow-indigo-500/40"
            placeholder="Enter your title here."
            name="title"
          />
          <textarea
            class="block px-5 mt-2 py-2 bg-black text-white resize-none w-full outline-none rounded-md shadow-lg shadow-indigo-500/40"
            placeholder="Write your task description here..."
            name="details"
          ></textarea>
          <input
            type="submit"
            class="mt-2 bg-black px-5 py-2 rounded text-white justify-center hover:bg-gray-500"
            value="Create Task"
          />
        </form>
      </div>

      <div class="tasks p-10 flex gap-5 flex-wrap">
        <% if (files.length > 0) { %> <% files.forEach((val) => { %>
        <div
          class="task w-72 px-3 py-4 bg-black rounded-md text-justify shadow-lg shadow-indigo-500/50"
        >
          <h1 class="text-white text-2xl"><%= val.title %></h1>
          <p class="text-white mt-3"><%= val.details %></p>
          <div class="flex justify-between items-center">
            <a
              class="text-blue-500 inline-block mt-3 hover:text-blue-600"
              href="/file/<%= val.title %>"
              >Read more</a
            >
            <a
              class="text-black inline-block mt-3 p-1 font-semibold bg-green-500 rounded hover:bg-green-600"
              href="/edit/<%= val.title %>"
              >Edit filename</a
            >
            <a
              class="inline-block mt-3 bg-red-500 rounded font-semibold text-black p-1 hover:bg-red-600"
              href="/delete/<%= val.title %>"
              >Delete</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600 text-4xl">No tasks yet.</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
